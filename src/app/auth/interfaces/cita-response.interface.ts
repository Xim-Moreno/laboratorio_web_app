export interface CitaResponse {
  // Define las propiedades que esperas en la respuesta
  id: string; // O cualquier otra propiedad que retorne el servidor
  message: string; // Un mensaje de éxito, si aplica
}
